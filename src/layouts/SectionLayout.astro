---
import type { HTMLAttributes } from "astro/types";
type Props = HTMLAttributes<"a">;

const { class: className, ...rest } = Astro.props;
---

<section {...rest} class:list={["mx-auto px-5 w-full lg:max-w-6xl", className]}>
  <slot />
</section>
